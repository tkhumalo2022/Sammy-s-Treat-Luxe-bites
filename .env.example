# PostgreSQL connection string from Supabase or another Postgres provider
DATABASE_URL=

# Generate a long random value; never commit the real secret
PAYLOAD_SECRET=

# Optional: enables AI-generated chatbot responses. Keep server-side only.
OPENAI_API_KEY=
